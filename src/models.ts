export interface Artist {
  id: number;
  name: number;
  ofId: number;
  alId: number;
  fnId: number;
}

export interface Event {
  id: number;
  location: string;
  name: string;
  publishedAt: string;
}
